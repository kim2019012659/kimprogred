#include <stdio.h>

struct book
{
	char title[30];
	char author[20];
	int page;
	int price;
};

int main(void)
{
	struct book s1;

	s1.title = h, b, s;
	s1.author = h, b, s;
	s1.page = 30, 40, 50;
	s1.price = 40, 50, 60;
	printf("title : %s,%s,%s\n, s1.title,s1.title,s1.title");
	printf("")


}